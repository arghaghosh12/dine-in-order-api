spring:
  application:
    name: Dine-in-order-api

  datasource:
    url: jdbc:mysql://localhost:3306/springboot_dine_in_order_db?createDatabaseIfNotExist=true
    username: root
    password: argha@@argha
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

